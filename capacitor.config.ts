import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ai_face_detection',
  appName: 'ai_face_detection',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
